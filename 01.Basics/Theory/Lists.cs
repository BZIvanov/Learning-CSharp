using System;

namespace LearnBasics
{
    class Lists
    {
        static void Main(string[] args)
        {
            // here we add values right after creating the list
            var values = new List<int>() { 1, 2, 3, 4 };

            var numbers = new List<int>();
            numbers.Add(1);
        }
    }
}